# TRATAMENTO DE DADOS E EXERCICIOS.
---
Por enquanto estamos utilizando var.
Nesta aula vamos exercitar os dois tipos primitivos: number, string.
---




## Comandos: 

```
var nome = window.prompt("") #nome guardado na variavel
window.alert("mensagem" + nome)
Number.parsInt() #converter pra inteiro
Number.parseFloat() #converter para float
Number(n) # decide sozinho
String(n) #converte para string
n.toString() #---\--
`mensagem ${string} mensagem ${numero} mensagem ${ever}` # template string
s.length \\ informa o numero de caracteres de um item
s.toUpperCase() \\ trasnforma em maisculas
document.write("mesnagem") \\ escreve no documento html
wirteln() \\quebra de linha
n1.toFixed(2) \\adiciona 2 casas decimais
n1.toFixed(2).replace('.',',') \\ virgula ao inves de ponto.
n1.toLocaleString('pt-BR', {style:'currency', currency:'BRL'}) \\ formatar para deinheiro


```

## Dicas

- diferencie concatenação de adição.
- cuidado com o uso da crase no placeholder.

## Para treinar: 

- Criar arquivo html, css e js num memso arquivo.
- O site que guarda nome  do window.prompt e imprimi no windows.alert.
- Com a mesma lógica dos itens de cima, cria uma lógica que faça duas somas.
- Treine no node, formatação de string.
- Progrma que lê idade, nome, nota  e imprimi com ${} 
- html que apresenta nome com a quatidade de caracteres de um nome, variações em maiusculo e minusculo.
- no node, fomamte numero com mais de uma casa decimal.
- formate um numero para dinheiro pt-BR.

