#include <stdio.h>
#include<string.h>


#define N 50

int main(void){

    char str1[N];
    char str2[N];

    printf("Digite um texto para variavel origem: ");
    
    gets(str1);
    gets(str2);

    printf()"Tamanhos: %d e %d", strlen(str1), strlen(str2);
    printf("Minusculas: %s")

    strcpy(destino, origem);
    printf("Texto origem: %s\nTexto destino: %s\n", origem, destino);

return 0;
}
